# Given strings s and t, return a list of all positions in which t occurs in s. For 
# example, if s = 'abcdeabcde' and t = 'cd', the result should be [2, 7] because
# 'cd' starts at index 2 and 7 inside string s. You may assume that t is not empty.
# Do not use the find method.

def allPositionsOfT(s, t):
 
   list1 = []
   for i in range(len(s)) :
        if t[0] == s[i]:
            list1.append(i)
   return list1
